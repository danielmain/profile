{
  "name": "danielmain",
  "version": "0.1.0",
  "scripts": {
    "build": "bsb -make-world",
    "compile": "bsb -make-world -w -ws _",
    "clean": "bsb -clean-world",
    "start": "moduleserve ./ --port 8080 --spa",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "reason-react": ">=0.9.1"
  },
  "devDependencies": {
    "bs-platform": "^8.3.3",
    "moduleserve": "^0.9.1"
  }
}
